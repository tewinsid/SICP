(define (d i)
	(if (temp i)
		(* 2 (/ (+ i 1) 3))
		1)
)
(define (temp j)
	(= (remainder (+ j) 3) 0)
)
(define n
	(lambda (i) 1.0))
(define (cont-frac n d k)
	(define (cf i)
		(if (= k i)
			(/ (n k) (d k))
			(/ (n i) (+ (d i) (cf (+ i 1))))
		)
	)
	(cf 1)
)
(define (test i)
	(+ 2 (cont-frac n d i))
)
